export type auctionItem = {
    id: string;
    make: string;
    model: string;
    year: number;
    description: string;
    end: string;
};

 export type timeRemaining = {
     seconds: number;
     minutes: number;
     hours: number;
     days: number;
 };
